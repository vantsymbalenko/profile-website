import React, { FC } from 'react';

import { HeaderSection } from '../../components/library/components/HeaderSection';

export const Contacts: FC = () => <HeaderSection sideText="Get in touch">Contacts</HeaderSection>;
